<%= bootstrap_form_with(model: history, local: true) do |form| %>
  <% if history.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(history.errors.count, "error") %> prohibited this booking from being saved:</h2>

      <ul>
      <% history.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.text_field :first_name, value: current_user.first_name %>
  </div>

  <div class="field">
    <%= form.text_field :last_name, value: current_user.last_name %>
  </div>

  <div class="field">
    <%= form.text_field :email, value: current_user.email %>
  </div>

  <div class="field">
    <%= form.telephone_field :number %>
  </div>

  <div class="field">
    <%= form.date_field :date %>
  </div>

  <div class="field">
    <%= form.time_field :time %>
  </div>

  <div class="field">
  <!-- Set up a range between 20 - 200 or there abouts ... somehting to do with in: 20..200 -->
    <%= form.number_field :number_of_heads  %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>