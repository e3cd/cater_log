<%= bootstrap_form_with(model: history, local: true) do |form| %>
  <% if history.errors.any? %>
    <div id="error_explanation">
    <h2><%= pluralize(review.errors.count, "error") %> prohibited this review from being saved:</h2>
      <ul>
      <% history.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.text_field :caterer_name, value: @caterer_name, label: "Caterer Name", readonly: true %>
  </div>

  <div>
    <%= form.text_field :menu, value: @menu, label: "Menu Selected", readonly: true %>
  </div>

  <div>
    <%= form.text_field :cost, value: @cost, label: "Cost Per Head", readonly: true %>
  </div>

  <div class="field">
    <%= form.text_field :first_name, value: current_user.first_name %>
  </div>

  <div class="field">
    <%= form.text_field :email, label: "Contact Email", value: current_user.email %>
  </div>

  <div class="field">
    <%= form.date_field :booking_date, autofocus: true %>
  </div>

  <div class="field">
  <!-- Set up a range between 20 - 200 or there abouts ... somehting to do with in: 20..200 -->
    <%= form.number_field :number_of_heads, label: "Number of people"  %>
  </div>

  <!-- passing caterer_menu.id through -->
  <%= form.hidden_field :caterer_menu_id, value: @caterer_menu_id  %>

  <div class="actions">
    <%= form.submit "Make Booking" %>
  </div>
  
<% end %>

